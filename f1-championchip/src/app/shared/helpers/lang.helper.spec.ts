import { LanguageHelper } from './lang.helper';

export class LangHelperMock {

}

describe('LanguageHelper', () => {
  it('should create an instance', () => {
    expect(new LanguageHelper()).toBeTruthy();
  });
});
